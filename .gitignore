# Rust + workspace
# Ignore any Cargo build dir anywhere (fixes nested crates like crates/*/target/)
target/

# Data artifacts live in Git LFS (see AGENTS.md)
# Keep local build caches out of git even though they live under data/
data/target/
data/bench/release/
data/bench/tmp/
data/bench/.rustc_info.json

# Stray/legacy Cargo target caches (must stay untracked)
data/target_seq*/
data/target_seq*/**

# mdBook build output (site is built by CI to gh-pages; sources live in docs/src)
/docs/book/

# OS/editor noise
**/.DS_Store
.vscode/

# Devcontainer local state (never versioned)
/.persist/

# Python: caches, virtualenvs, build outputs
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
.venv/
dist/
build/
*.egg-info/

# Note: we intentionally commit the built abi3 native module
# (src/viterbo/viterbo_native*.so) together with a .run.json stamp so the repo
# is self-contained for agents. Do not add ignore rules for that file.
